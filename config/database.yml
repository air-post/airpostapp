# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: 5
  timeout: 5000

development:
  adapter: postgresql 
  encoding: utf8
  pool: 5

  host: localhost
  port: 5432
  database: airpost_dev
  username: postgres
  password: postgres
  # adapter: postgresql
  # encoding: unicode
  # username: postgres
  # password: postgres
  # host: localhost
  # port: 5432
  # database: airpost_dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2 
  encoding: utf8
  pool: 5

  host: mysql.airpost.dreamhosters.com
  port: 3306
  database: airpost_dev
  username: airpost
  password: Pa55w0rd

production:
  <<: *default
  database: db/production.sqlite3
